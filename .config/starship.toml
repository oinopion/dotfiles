# Docs: https://starship.rs/config/

# Git takes a long time in dev container
command_timeout = 20000
right_format = """$time"""

# Status module displays exit code for failed commands
[status]
disabled = false

# Container module displays a container name if inside the container
[container]
disabled = true

[git_status]
stashed = ''  # Don't show if there are stashed changes (there usually are)

[gcloud]
symbol = " "

[time]
disabled = false
